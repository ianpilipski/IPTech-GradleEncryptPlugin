buildscript {
    dependencies {
        classpath group: 'com.iptech.gradle', name: 'encrypt-plugin'
    }
}

apply plugin: 'com.iptech.gradle.encrypt-plugin'


encrypt {
    password = 'dood'
    encryptedFiles {
        from = project.layout.dir('myEncryptedDir')
    } 
}